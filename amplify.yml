version: 0.1
frontend:
  phases:
    pre-build:
      commands:
        - yarn install
    build:
      commands:
        - yarn scully:build
  artifacts:
    baseDirectory: dist/static
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
